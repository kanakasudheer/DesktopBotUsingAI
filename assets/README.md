# Assets Directory

This directory contains assets used by the JARVIS AI Assistant interface.

## Files
- `background.jpg` - High-resolution 4K background image for the JARVIS interface

The background image will be automatically downloaded on first run if not present.